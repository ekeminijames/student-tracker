{
  "name": "emi-lms",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @emi/web",
    "build": "npm run build -w @emi/web",
    "preview": "npm run preview -w @emi/web",
    "typecheck": "npm run typecheck -w @emi/shared && npm run typecheck -w @emi/web",
    "lint": "echo \"(lint is configured in a later tier)\""
  }
}
